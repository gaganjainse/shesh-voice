# Shesh Voice default settings (drop into ~/.config/Shesh/settings.ini)
[llm]
provider = ollama
model = phi4-mini:latest
base_url = http://localhost:11434/v1

[stt]
engine = openwakeword
wake_word = hey shesh

[mcp]
servers_file = ~/.config/Shesh/mcp-servers.json
